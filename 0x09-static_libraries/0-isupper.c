#include "main.h"
/**
* _isupper - the function for chek the upper
* @c: is a var
* Return: the returning value is 1 if it c upper else 0
*/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	return (1);
	return (0);
}
